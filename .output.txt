L1: import { useEffect, useRef, useState } from 'react'
L2: import { useStore } from './store/useStore.js'
L3: import { useUI } from './store/useUI.js'
L4: import { EXDB, EXIDX, BODYPARTS, isCardio, isBodyweightEq, allExercises, equipmentOf, smOf, matchExercise, exOr } from './lib/exercises.js'
L5: import { activeProfile, exAvailable, ALL_EQUIPMENT, newProfile } from './lib/equipment.js'
L6: import { fmtDate, fmtNum, fmtVol, fmtDur, durPart, todayISO, uid, exCount, DAYN, MONTHS_LONG, ACCENTS } from './lib/format.js'
L7: import { lastEntryFor, bestWeightFor, buildSets, effectiveRoutineId, workoutVolume, setsDone, setsDoneActive, lastBW, supersetUnits, unitOf, setLabel, defaultConfig, cleanupSg, modeOf, effortOf, isBw, isPerSide, sideReps, workSetsDone, applyIntensifierPlan, MAX_PLANNED_WARMUPS, NOTE_MAX } from './lib/history.js'
L8: import { beep, vibrate } from './lib/sound.js'
L9: import { t, instrFor, exerciseNameFor, getLang, INSTR_LANGS } from './lib/i18n.js'
L10: import { nav } from './lib/nav.js'
L11: import { starterRoutines, routinesFromPresets } from './lib/starter.js'
L12: import { api } from './lib/api.js'
L13: import Media, { Thumb } from './components/Media.jsx'
L14: import Stepper from './components/Stepper.jsx'
L15: import Icon from './components/Icon.jsx'
L16: import { Button, Slider, Switch, Segmented, SelectRow, Row, TextField, MultiSelectRow } from './components/ui.jsx'
L17: import { glyphOf, GLYPH_GROUPS, DEFAULT_GLYPH } from './lib/glyphs.js'
L18: import BodyMap from './components/BodyMap.jsx'
L19: import { exerciseMuscleSnapshot, loadOfWorkouts, MUSCLES, MUSCLE_NAME, normalizeMuscleGroups, hasExplicitMuscleMetadata } from './lib/muscles.js'
L20: import { parseImport, mergeImport } from './lib/import-csv.js'
L21: import { buildPlanBundle, parsePlan, mergePlan, printPlan } from './lib/plan-share.js'
L22: import { estimate1RM, best1RM, is1RMRecord, REP_CAP } from './lib/onerm.js'
L23: import { nextPrescription, applyPrescription, policyFor, defaultIncrement, POLICIES_FOR, POLICY_NAME, POLICY_DESC, MAX_BW_SETS } from './lib/progression.js'
L24: import { buildCompletedWorkout } from './lib/finish-workout.js'
L25: import { isWarmupRow } from './lib/workout-model.js'
L26: 
L27: const S = () => useStore.getState().S
L28: const update = (...a) => useStore.getState().update(...a)
L29: const ui = () => useUI.getState()
L30: const toast = m => ui().toast(m)
L31: const snd = () => S().sound
L32: 
L33: /* ============================ custom confirm dialog ============================ */
L34: function ConfirmDialog({ title, message, confirmText, cancelText, danger, onConfirm, close }) {
L35:   return <div style={{ textAlign: 'center', padding: '4px 0' }}>
L36:     {title && <h3 style={{ marginBottom: 8 }}>{title}</h3>}
L37:     <div className="muted" style={{ marginBottom: 18, lineHeight: 1.5 }}>{message}</div>
L38:     <button className={'btn ' + (danger ? 'danger' : 'primary')} onClick={() => { close(); onConfirm && onConfirm() }}>{confirmText || t('Confirm')}</button>
L39:     <div style={{ height: 8 }} />
L40:     <Button variant="ghost" className="dim" onClick={close}>{cancelText || t('Cancel')}</Button>
L41:   </div>
L42: }
L43: // Themed replacement for window.confirm — callback-based (no blocking).
L44: export function confirmSheet(opts) {
L45:   ui().openSheet(close => <ConfirmDialog {...opts} close={close} />, { kind: 'center' })
L46: }
L47: 
L48: /* ============================ starter plan ============================ */
L49: export async function loadStarterPlan() {
L50:   let routines = null
L51:   if (useStore.getState().user) {
L52:     try {
L53:       const result = await api('/api/presets')
L54:       routines = routinesFromPresets(result.presets)
L55:     } catch (e) { /* use the built-in fallback when the server is unavailable */ }
L56:   }
L57:   if (!routines?.length) routines = starterRoutines()
L58:   const [push, pull, legs] = routines
L59:   update(st => {
L60:     st.routines.push(...routines)
L61:     if (push) st.week[1] = push.id
L62:     if (pull) st.week[3] = pull.id
L63:     if (legs) st.week[5] = legs.id
L64:     st.estadoInicial = 'plan_predeterminado'
L65:   })
L66:   toast(t('Starter plan loaded'))
L67: }
L68: 
L69: /* ============================ weight picker (shared: body weight + goal) ============================ */
L70: // Fixed range, not a moving window — a window that resizes itself mid-drag (the previous
L71: // attempt) makes the thumb's position unpredictable: every time it grows, everything already
L72: // placed on it shifts toward one side. A static range never has that problem, at the cost of
L73: // coarser precision per pixel — the +/- buttons cover exact values.
L74: // The ceiling follows the profile's unit: 300 covers a body weight or a working weight in
L75: // kg, but as pounds it cut off at 136 kg — below plenty of people's body weight, and well
L76: // below an everyday squat.
L77: const W_LO = 1
L78: const wHi = unit => (unit === 'lb' ? 660 : 300)
L79: function WeightInput({ value, setValue, unit }) {
L80:   const W_HI = wHi(unit)
L81:   const clamp = x => Math.max(W_LO, Math.min(W_HI, Math.round((x || 0) * 10) / 10))
L82:   const sv = Math.max(W_LO, Math.min(W_HI, value))
L83:   const onSlide = v => setValue(clamp(v))
L84:   return <>
L85:     <div className="bwstep">
L86:       <button className="bw-pm" onClick={() => onSlide(value - 0.1)} aria-label="minus 0.1"><Icon name="minus" /></button>
L87:       <div className="bw-read">{fmtNum(value)}<span className="u"> {unit}</span></div>
L88:       <button className="bw-pm" onClick={() => onSlide(value + 0.1)} aria-label="plus 0.1"><Icon name="plus" /></button>
L89:     </div>
L90:     <div className="chips" style={{ justifyContent: 'center', margin: '8px 0' }}>
L91:       <button className="chip" onClick={() => onSlide(value - 1)}>−1</button>
L92:       <button className="chip" onClick={() => onSlide(value - 0.5)}>−0.5</button>
L93:       <button className="chip" onClick={() => onSlide(value + 0.5)}>+0.5</button>
L94:       <button className="chip" onClick={() => onSlide(value + 1)}>+1</button>
L95:     </div>
L96:     <Slider value={sv} min={W_LO} max={W_HI} step={0.5} onChange={onSlide} />
L97:   </>
L98: }
L99: 
L100: /* ============================ body weight ============================ */
L101: function BwSheet({ required, onDone, close }) {
L102:   const st = useStore(s => s.S)
L103:   const unit = st.unit
L104:   const bw = lastBW(st)
L105:   const [v, setV] = useState(bw ? bw.w : 70)
L106:   const doneRef = useRef(false)
L107: 
L108:   const pedirPeso = st.configuracion?.pedirPesoAlEntrenar !== false
L109:   const togglePedirPeso = (val) => {
L110:     update(s => {
L111:       s.configuracion = { ...(s.configuracion || {}), pedirPesoAlEntrenar: val }
L112:     })
L113:   }
L114: 
L115:   const finish = (weightVal) => {
L116:     if (doneRef.current) return
L117:     doneRef.current = true
L118:     close()
L119:     if (onDone) onDone(weightVal)
L120:     else toast(t('Weight saved'))
L121:   }
L122: 
L123:   const save = () => {
L124:     const n = Math.round((v || 0) * 10) / 10
L125:     if (!n || n <= 0) { toast(t('Enter a valid weight')); return }
L126:     update(s => {
L127:       const iso = todayISO()
L128:       const ex = s.bodyweight.find(b => b.d === iso)
L129:       if (ex) { ex.w = n; ex.t = Date.now() } else s.bodyweight.push({ d: iso, w: n, t: Date.now() })
L130:       s.bodyweight.sort((a, b) => (a.d < b.d ? -1 : 1))
L131:     })
L132:     finish(n)
L133:   }
L134: 
L135:   const cancel = () => {
L136:     if (doneRef.current) return
L137:     doneRef.current = true
L138:     close()
L139:   }
L140: 
L141:   const iniciarSinPesarse = () => {
L142:     finish(null)
L143:   }
L144: 
L145:   useEffect(() => {
L146:     return () => {
L147:       doneRef.current = true
L148:     }
L149:   }, [])
L150: 
L151:   const recent = [...st.bodyweight].reverse().slice(0, 3)
L152:   const delEntry = d => update(s => { s.bodyweight = s.bodyweight.filter(b => b.d !== d) })
L153:   return <>
L154:     <div style={{ position: 'relative', paddingRight: 32 }}>
L155:       <h3>{required ? t('Quick check-in') : t('Log body weight')}</h3>
L156:       <button
L157:         className="iconbtn"
L158:         style={{ position: 'absolute', top: -4, right: -4, color: 'var(--txt)', background: 'var(--surface-2)' }}
L159:         onClick={cancel}
L160:         aria-label="Close"
L161:       >
L162:         <Icon name="xmark" />
L163:       </button>
L164:     </div>
L165: 
L166:     {required && <div className="row between" style={{ padding: '8px 0', borderBottom: '1px solid var(--sep)', marginBottom: 12 }}>
L167:       <span className="small muted">{t('Pedir peso al iniciar entrenamiento')}</span>
L168:       <Switch checked={pedirPeso} onChange={togglePedirPeso} />
L169:     </div>}
L170: 
L171:     <div className="muted small">{required ? t('Slide or tap to set your weight — tracked before every workout so your curve stays honest.') : t('Today') + ', ' + fmtDate(todayISO(), true)}</div>
L172:     <WeightInput value={v} setValue={setV} unit={unit} />
L173:     <div style={{ height: 14 }} />
L174:     <Button variant="primary" onClick={save}>{required ? t('Save & start workout') : t('Save')}</Button>
L175:     {required && <>
L176:       <div style={{ height: 8 }} /><Button variant="ghost" className="dim" onClick={iniciarSinPesarse}>{t('Start without weighing in')}</Button>
L177:       <div style={{ height: 2 }} /><Button variant="ghost" className="dim" icon="reset" onClick={() => { doneRef.current = true; close(); nav('/workout') }}>{t('Choose a different workout')}</Button>
L178:     </>}
L179:     {!required && recent.length > 0 && <>
L180:       <h4 className="sec">{t('Recent weigh-ins')}</h4>
L181:       <div className="list" style={{ gap: 0 }}>
L182:         {recent.map(b => <div key={b.d} className="row between" style={{ padding: '9px 2px', borderBottom: '1px solid var(--sep)' }}>
L183:           <span className="small muted">{fmtDate(b.d, true)}</span>
L184:           <span className="row" style={{ gap: 12 }}><b>{fmtNum(b.w)} {unit}</b>
L185:             <button className="iconbtn" style={{ width: 32, height: 30, borderRadius: 8, fontSize: 15, color: 'var(--red)' }} onClick={() => delEntry(b.d)} aria-label="delete"><Icon name="trash" /></button></span>
L186:         </div>)}
L187:       </div>
L188:     </>}
L189:   </>
L190: }
L191: export function bwSheet(opts = {}) {
L192:   const h = ui().openSheet(close => <BwSheet {...opts} close={close} />, { locked: false })
L193:   return h
L194: }
L195: 
L196: /* ============================ import from another app ============================ */
L197: // Shows what a parsed export would actually do before anything is written. An import is
L198: // the one action where "just try it" is expensive — it's someone's entire training
L199: // history — so the numbers, the unit conversion and the exercises we couldn't recognise
L200: // are all on screen before the confirm button.
L201: function ImportSummary({ parsed, close }) {
L202:   const st = useStore(s => s.S)
L203:   const isBW = parsed.kind === 'bodyweight'
L204:   const have = isBW
L205:     ? parsed.bodyweight.filter(b => st.bodyweight.some(x => x.d === b.d)).length
L206:     : parsed.workouts.filter(w => st.workouts.some(x => x.d === w.d)).length
L207:   const fresh = (isBW ? parsed.bodyweight.length : parsed.workouts.length) - have
L208: 
L209:   const doImport = () => {
L210:     let res
L211:     update(s => { res = mergeImport(s, parsed) })
L212:     close()
L213:     toast(isBW
L214:       ? t('{0} weigh-ins imported', res.added)
L215:       : t('{0} workouts imported', res.added))
L216:   }
L217: 
L218:   return <>
L219:     <h3>{parsed.source ? t('Import from {0}', parsed.source) : t('Import history')}</h3>
L220:     <div className="muted small" style={{ marginBottom: 12 }}>
L221:       {parsed.from === parsed.to ? fmtDate(parsed.from, true) : fmtDate(parsed.from, true) + ' – ' + fmtDate(parsed.to, true)}
L222:     </div>
L223: 
L224:     <div className="tiles" style={{ textAlign: 'left' }}>
L225:       {isBW ? <>
L226:         <div className="tile"><div className="l">{t('Weigh-ins')}</div><div className="v" style={{ fontSize: '1.1rem' }}>{parsed.bodyweight.length}</div></div>
L227:         <div className="tile"><div className="l">{t('New')}</div><div className="v" style={{ fontSize: '1.1rem' }}>{fresh}</div></div>
L228:       </> : <>
L229:         <div className="tile"><div className="l">{t('Workouts')}</div><div className="v" style={{ fontSize: '1.1rem' }}>{parsed.workouts.length}</div></div>
L230:         <div className="tile"><div className="l">{t('Sets')}</div><div className="v" style={{ fontSize: '1.1rem' }}>{parsed.sets}</div></div>
L231:         <div className="tile"><div className="l">{t('Exercises matched')}</div><div className="v" style={{ fontSize: '1.1rem' }}>{parsed.matched}</div></div>
L232:         <div className="tile"><div className="l">{t('Added as your own')}</div><div className="v" style={{ fontSize: '1.1rem' }}>{parsed.created}</div></div>
L233:       </>}
L234:     </div>
L235: 
L236:     {parsed.mixedUnits ? <div className="small" style={{ color: 'var(--yellow)', marginBottom: 10 }}>
L237:       {t('The file mixes kg and lb — each set is converted to {0}.', st.unit)}
L238:     </div> : parsed.converted ? <div className="small" style={{ color: 'var(--yellow)', marginBottom: 10 }}>
L239:       {t('The file is in {0} and your profile is in {1} — weights will be converted.', parsed.fileUnit, st.unit)}
L240:     </div> : null}
L241:     {!isBW && !parsed.fileUnit && !parsed.mixedUnits && <div className="small dim" style={{ marginBottom: 10 }}>
L242:       {t('The file does not say which unit it uses — numbers are imported as they are.')}
L243:     </div>}
L244:     {have > 0 && <div className="small dim" style={{ marginBottom: 10 }}>
L245:       {t('{0} days already have data here and will be left alone.', have)}
L246:     </div>}
L247:     {/* The file rated its sets. Say so: the column is off by default, so the ratings would
L248:         otherwise arrive invisibly and look like they had been dropped. */}
L249:     {!isBW && (parsed.rirSets + parsed.rpeSets) > 0 && <div className="small dim" style={{ marginBottom: 10 }}>
L250:       {t(effortOf(st) === 'none'
…884 lines truncated…
L1135:   if (!pedirPeso) {
L1136:     beginWorkout(routineId, null)
L1137:     return
L1138:   }
L1139:   bwSheet({ required: true, onDone: bw => beginWorkout(routineId, bw) })
L1140: }
L1141: export function beginWorkout(routineId, bw) {
L1142:   const st = S()
L1143:   const r = routineId ? st.routines.find(x => x.id === routineId) : null
L1144:   // The prescription is applied as the session is built, so you walk up to the bar with the
L1145:   // right weight already on the screen instead of being told about it afterwards. `plan` is
L1146:   // kept on the entry purely so the workout can explain the number it chose.
L1147:   const entries = (r ? r.ex : []).map(cfg => {
L1148:     const plan = nextPrescription(st, cfg, r)
L1149:     const step = defaultIncrement(cfg.id, st.unit)
L1150:     const sets = applyIntensifierPlan(applyPrescription(buildSets(st, cfg, { step }), plan, step), cfg)
L1151:     return { id: cfg.id, sg: cfg.sg, target: { ...cfg }, plan, sets }
L1152:   })
L1153:   update(s => {
L1154:     s.active = { id: uid(), d: todayISO(), start: Date.now(), routineId, name: r ? r.name : t('Freestyle'), bw: bw || null, cur: 0, entries }
L1155:   })
L1156:   useUI.getState().stopRest()
L1157:   nav('/workout')
L1158: }
L1159: function TopWeight({ entryIdx, close }) {
L1160:   const st = useStore(s => s.S)
L1161:   const A = st.active
L1162:   // The workout can end underneath this sheet: finishing from the last exercise clears
L1163:   // `active`, and this re-renders before the sheet is torn down. Everything below is
L1164:   // read defensively and the sheet dismisses itself — reading A.entries straight took
L1165:   // the whole app down with it. Hooks still run unconditionally, so the bail-out has
L1166:   // to sit after every one of them.
L1167:   const entry = A ? A.entries[entryIdx] : null
L1168:   const ex = entry && EXIDX[entry.id]
L1169:   const maxSet = entry ? Math.max(0, ...entry.sets.filter(s => s.done && !isWarmupRow(s)).map(s => s.w || 0)) : 0
L1170:   const prevBest = entry ? Math.max((st.exWeights[entry.id] || {}).w || 0, bestWeightFor(st, entry.id)) : 0
L1171:   const [v, setV] = useState(entry ? (Math.max(maxSet, prevBest) || entry.target.weight || 0) : 0)
L1172:   useEffect(() => { if (!entry) close() }, [!entry])
L1173: 
L1174:   const units = supersetUnits(A ? A.entries : [])
L1175:   const unit = entry ? unitOf(units, entryIdx) : []
L1176:   const unitDone = !!entry && unit.every(i => A.entries[i].sets.every(s => s.done))
L1177:   const unitIdx = units.findIndex(u => u === unit)
L1178:   const isLastUnit = unitIdx === units.length - 1
L1179:   if (!entry || !ex) return null
L1180: 
L1181:   const commit = advance => {
L1182:     const n = Math.round((v || 0) * 10) / 10
L1183:     if (!isFinite(n) || n < 0) { toast(t('Enter a valid weight')); return }
L1184:     update(s => {
L1185:       s.active.entries[entryIdx].topW = n
L1186:       const cur = s.exWeights[entry.id]
L1187:       s.exWeights[entry.id] = { w: Math.max(n, cur ? cur.w : 0), d: todayISO() }
L1188:     })
L1189:     close()
L1190:     if (advance && unitDone) {
L1191:       if (isLastUnit) workoutCompleteSheet()               // whole workout done → finish/continue prompt
L1192:       else update(s => { s.active.cur = units[unitIdx + 1][0] })
L1193:     } else toast(t('Tracked — next time starts at {0}', fmtNum(S().exWeights[entry.id].w) + ' ' + st.unit))
L1194:   }
L1195:   return <>
L1196:     <h3 className="capitalize row" style={{ gap: 8 }}><Icon name="checkCircle" style={{ color: 'var(--acc)' }} />{t('{0} done', exerciseNameFor(ex))}</h3>
L1197:     <div className="muted small">{t('Confirm the weight you worked with — your highest becomes the default next time.')}{!unitDone && unit.length > 1 ? ' ' + t('Then finish the superset partner.') : ''}</div>
L1198:     <WeightInput value={v} setValue={setV} unit={st.unit} />
L1199:     <div style={{ height: 10 }} />
L1200:     {prevBest > 0 ? <div className="small dim" style={{ textAlign: 'center', marginBottom: 12 }}>{t('Previous best:')} {fmtNum(prevBest)} {st.unit}{maxSet > prevBest && <span style={{ color: 'var(--yellow)' }}> — {t('new record!')}</span>}</div> : <div style={{ height: 4 }} />}
L1201:     {unitDone ? <>
L1202:       <Button variant="primary" trailingIcon={isLastUnit ? null : 'chevronRight'} onClick={() => commit(true)}>{isLastUnit ? t('Save') : t('Save & next exercise')}</Button>
L1203:       <div style={{ height: 8 }} /><Button variant="ghost" className="dim" onClick={() => commit(false)}>{t('Just close')}</Button>
L1204:     </> : <Button variant="primary" onClick={() => commit(false)}>{t('Save weight')}</Button>}
L1205:   </>
L1206: }
L1207: export const topWeightSheet = entryIdx => ui().openSheet(close => <TopWeight entryIdx={entryIdx} close={close} />)
L1208: 
L1209: /* ============================ exercise notes ============================
L1210:    Two notes, one sheet, because from the user's side it is one question — "what do I want to
L1211:    remember about this exercise?" — with two different lifetimes:
L1212: 
L1213:      · today's note belongs to this session and is stored on the workout entry. It is history:
L1214:        what happened, how it felt. The PIN is the user saying "this one is for next time", which
L1215:        only they can know at the moment of writing — see pinnedNoteFor.
L1216:      · the standing note belongs to the exercise itself and lives in S.exNotes. Seat height, pin
L1217:        position, a form cue. True every session, so it is shown every session and never expires.
L1218: 
L1219:    A routine's own `note` (a plan's instruction for this exercise) is edited in the config sheet
L1220:    and is deliberately not here: it belongs to the plan, not to the day or to the movement. */
L1221: function ExerciseNote({ entryIdx, close }) {
L1222:   const st = useStore(s => s.S)
L1223:   const update = useStore(s => s.update)
L1224:   const A = st.active
L1225:   const entry = A ? A.entries[entryIdx] : null
L1226:   const ex = entry ? exOr(entry.id) : null
L1227:   const [note, setNote] = useState(entry?.note || '')
L1228:   const [pin, setPin] = useState(!!entry?.notePin)
L1229:   const [standing, setStanding] = useState(entry ? (st.exNotes?.[entry.id] || '') : '')
L1230:   useEffect(() => { if (!entry) close() }, [!entry])
L1231:   if (!entry) return null
L1232: 
L1233:   const save = () => {
L1234:     const today = note.trim().slice(0, NOTE_MAX)
L1235:     const always = standing.trim().slice(0, NOTE_MAX)
L1236:     update(s => {
L1237:       const e = s.active?.entries?.[entryIdx]
L1238:       if (e) {
L1239:         if (today) { e.note = today; if (pin) e.notePin = true; else delete e.notePin }
L1240:         else { delete e.note; delete e.notePin }
L1241:       }
L1242:       s.exNotes = s.exNotes || {}
L1243:       if (always) s.exNotes[entry.id] = always
L1244:       else delete s.exNotes[entry.id]
L1245:     })
L1246:     close()
L1247:   }
L1248: 
L1249:   return <>
L1250:     <h3 className="capitalize">{exerciseNameFor(ex)}</h3>
L1251:     <div className="small muted" style={{ marginBottom: 6 }}>{t('This session')}</div>
L1252:     <textarea className="input" rows={3} maxLength={NOTE_MAX} value={note}
L1253:       placeholder={t('How it went, what to change — kept with today’s workout.')}
L1254:       onChange={e => setNote(e.target.value)} />
L1255:     <div style={{ height: 10 }} />
L1256:     <div className="sect-b">
L1257:       <Row icon="flag" iconTint="var(--yellow)" title={t('Show this next time')}
L1258:         subtitle={t('Brings it up again the next time you train this exercise.')}>
L1259:         <Switch checked={pin} onChange={setPin} disabled={!note.trim()} />
L1260:       </Row>
L1261:     </div>
L1262:     <div style={{ height: 18 }} />
L1263:     <div className="small muted" style={{ marginBottom: 6 }}>{t('Always for this exercise')}</div>
L1264:     <textarea className="input" rows={2} maxLength={NOTE_MAX} value={standing}
L1265:       placeholder={t('Seat height, pin position, a form cue — shown every session.')}
L1266:       onChange={e => setStanding(e.target.value)} />
L1267:     <div style={{ height: 18 }} />
L1268:     <Button variant="primary" onClick={save}>{t('Save')}</Button>
L1269:   </>
L1270: }
L1271: export const exerciseNoteSheet = entryIdx => ui().openSheet(close => <ExerciseNote entryIdx={entryIdx} close={close} />)
L1272: 
L1273: /* The session note: how the whole workout went, as opposed to how one exercise went. It lives
L1274:    on the active session, so buildCompletedWorkout carries it onto the finished workout and it
L1275:    shows up again in history — where it stays editable. Written here rather than only after the
L1276:    fact because "notes you can write during a workout" is the point; a note you can only add
L1277:    once the session is filed is a different, smaller feature. */
L1278: function SessionNote({ close }) {
L1279:   const st = useStore(s => s.S)
L1280:   const update = useStore(s => s.update)
L1281:   const A = st.active
L1282:   const [note, setNote] = useState(A?.note || '')
L1283:   useEffect(() => { if (!A) close() }, [!A])
L1284:   if (!A) return null
L1285: 
L1286:   const save = () => {
L1287:     const text = note.trim().slice(0, NOTE_MAX)
L1288:     update(s => { if (!s.active) return; if (text) s.active.note = text; else delete s.active.note })
L1289:     close()
L1290:   }
L1291: 
L1292:   return <>
L1293:     <h3>{t('Session note')}</h3>
L1294:     <textarea className="input" rows={4} maxLength={NOTE_MAX} value={note}
L1295:       placeholder={t('How the session went as a whole.')}
L1296:       onChange={e => setNote(e.target.value)} />
L1297:     <div style={{ height: 18 }} />
L1298:     <Button variant="primary" onClick={save}>{t('Save')}</Button>
L1299:   </>
L1300: }
L1301: export const sessionNoteSheet = () => ui().openSheet(close => <SessionNote close={close} />)
L1302: 
L1303: /* Drop-set drops and rest-pause bursts are edited inline on the set row itself (Workout.jsx) —
L1304:    no sheet, no timer. A planned exercise (see the "Intensifier" config below) arrives with them
L1305:    already computed via applyIntensifierPlan; an unplanned straight set can still grow one live
L1306:    by tapping "+ Drop"/"+ Burst", which appends with the same suggested-next-value math. */
L1307: 
L1308: // Shown when the last exercise's last set is checked — finish, or keep going.
L1309: function WorkoutComplete({ close }) {
L1310:   return <div style={{ textAlign: 'center', padding: '8px 0' }}>
L1311:     <div style={{ fontSize: 44, display: 'flex', justifyContent: 'center', color: 'var(--acc)' }}><Icon name="checkCircle" /></div>
L1312:     <h3 style={{ margin: '8px 0' }}>{t("That's the whole workout!")}</h3>
L1313:     <div className="muted small" style={{ marginBottom: 16 }}>{t('Every exercise done — great work. Finish up, or keep going and add another exercise.')}</div>
L1314:     <Button variant="primary" icon="flag" onClick={() => { close(); finishWorkout() }}>{t('Finish workout')}</Button>
L1315:     <div style={{ height: 8 }} />
L1316:     <Button onClick={() => { close(); useUI.getState().toast(t('Keep going — tap “+ Add exercise” below')) }}>{t('Continue workout')}</Button>
L1317:   </div>
L1318: }
L1319: export const workoutCompleteSheet = () => ui().openSheet(close => <WorkoutComplete close={close} />, { kind: 'center' })
L1320: 
L1321: function FinishSummary({ w, prs, e1prs = [], close }) {
L1322:   const st = useStore(s => s.S)
L1323:   return <div style={{ textAlign: 'center', padding: '8px 0' }}>
L1324:     <div style={{ fontSize: 44, display: 'flex', justifyContent: 'center', color: 'var(--acc)' }}><Icon name="trophy" /></div>
L1325:     <h3 style={{ margin: '8px 0' }}>{t('Workout complete!')}</h3>
L1326:     <div className="tiles" style={{ textAlign: 'left' }}>
L1327:       <div className="tile"><div className="l">{t('Duration')}</div><div className="v" style={{ fontSize: '1.1rem' }}>{fmtDur(w.end - w.start)}</div></div>
L1328:       <div className="tile"><div className="l">{t('Volume')}</div><div className="v" style={{ fontSize: '1.1rem' }}>{fmtVol(w.vol, st.unit)}</div></div>
L1329:       <div className="tile"><div className="l">{t('Sets')}</div><div className="v" style={{ fontSize: '1.1rem' }}>{t('{0} sets · {1} work', setsDone(w), workSetsDone(w))}</div></div>
L1330:       <div className="tile"><div className="l">{t('PRs')}</div><div className="v" style={{ fontSize: 20 }}>{prs.length || '—'}</div></div>
L1331:     </div>
L1332:     {(prs.length > 0 || e1prs.length > 0) && <div style={{ textAlign: 'left', marginBottom: 12 }}>
L1333:       {prs.map(id => <div key={id} className="small accent capitalize row" style={{ gap: 5 }}><Icon name="trophy" style={{ fontSize: 13 }} />{t('New PR:')} {EXIDX[id] ? exerciseNameFor(EXIDX[id]) : id}</div>)}
L1334:       {e1prs.map(p => <div key={p.id} className="small accent capitalize row" style={{ gap: 5 }}><Icon name="chartLine" style={{ fontSize: 13 }} />{t('Best estimated 1RM:')} {EXIDX[p.id] ? exerciseNameFor(EXIDX[p.id]) : p.id} · {fmtNum(p.est)} {st.unit}</div>)}
L1335:     </div>}
L1336:     <h4 className="sec" style={{ textAlign: 'left' }}>{t('What you just trained')}</h4>
L1337:     <BodyMap load={loadOfWorkouts([w])} body={st.body} />
L1338:     <div style={{ height: 14 }} />
L1339:     <Button variant="primary" onClick={() => { close(); nav('/home') }}>{t('Nice!')}</Button>
L1340:   </div>
L1341: }
L1342: export function finishWorkout() {
L1343:   const A = S().active
L1344:   if (!A) return
L1345:   const done = setsDoneActive(A)
L1346:   const total = A.entries.reduce((n, e) => n + e.sets.length, 0)
L1347:   if (!done) { confirmSheet({ title: t('Nothing logged yet'), message: t('You haven’t checked off any sets. Finish the workout anyway?'), confirmText: t('Finish anyway'), onConfirm: doFinishWorkout }); return }
L1348:   if (done < total) { confirmSheet({ title: t('Finish early?'), message: t(total - done === 1 ? '{0} set still unchecked. Finish the workout now?' : '{0} sets still unchecked. Finish the workout now?', total - done), confirmText: t('Finish workout'), onConfirm: doFinishWorkout }); return }
L1349:   doFinishWorkout()
L1350: }
L1351: function doFinishWorkout() {
L1352:   const st = S()
L1353:   const A = st.active
L1354:   if (!A) return
L1355:   const prs = []
L1356:   const e1prs = []
L1357:   A.entries.forEach(e => {
L1358:     const mx = Math.max(0, ...e.sets.filter(s => s.done && !isWarmupRow(s)).map(s => s.w))
L1359:     if (mx > 0 && mx > bestWeightFor(st, e.id)) prs.push(e.id)
L1360:     // A heavier estimate without a heavier top set is its own kind of progress —
L1361:     // same weight for more reps. Reported separately so it can't be read as a load PR.
L1362:     const rec = is1RMRecord(st, e.id, e)
L1363:     if (rec && !prs.includes(e.id)) e1prs.push({ id: e.id, ...rec })
L1364:   })
L1365:   const w = buildCompletedWorkout(A, {
L1366:     end: Date.now(),
L1367:     prs,
L1368:     snapshotFor: e => EXIDX[e.id]?.custom ? exerciseMuscleSnapshot(EXIDX[e.id]) : null,
L1369:   })
L1370:   w.vol = workoutVolume(w)
L1371:   update(s => {
L1372:     w.entries.forEach(e => {
L1373:       const mx = Math.max(0, ...e.sets.filter(x => x.done && !isWarmupRow(x)).map(x => x.w || 0), e.topW || 0)
L1374:       if (mx > 0) { const cur = s.exWeights[e.id]; if (!cur || mx > cur.w) s.exWeights[e.id] = { w: mx, d: w.d } }
L1375:     })
L1376:     s.workouts.push(w)
L1377:     s.active = null
L1378:   })
L1379:   useStore.getState().autoBackupNow()
L1380:   useUI.getState().stopRest()
L1381:   beep(snd(), 880, 0.15); beep(snd(), 1100, 0.15, 0.18); beep(snd(), 1320, 0.3, 0.36)
L1382:   ui().openSheet(close => <FinishSummary w={w} prs={prs} e1prs={e1prs} close={close} />, { kind: 'center', locked: true })
L1383: }
L1384: 
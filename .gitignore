# Exercise media — fetched on first `docker compose up` (or via scripts/fetch-media.sh)
media/img/
media/gif/

# Runtime data — users, passkeys, per-user state, session secret
data/
!data/db_espanol.json
# Local config
.env
.env.example
*.pem


# Cloudflare Tunnels credentials
/etc/cloudflared/
*.json
!package.json
!package-lock.json

# Build output / deps
frontend/dist/
.idea/
.pnpm-store/
node_modules/
*.log
.DS_Store
node_modules
.claude/
.agents/
docker-compose.yml
